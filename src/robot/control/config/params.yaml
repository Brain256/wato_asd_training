control_node:
  ros__parameters:
    # Pure Pursuit Controller parameters
    lookahead_distance: 1.2   # Distance ahead to look for path point (meters)
    linear_speed: 0.8          # Constant forward speed (m/s)
    goal_tolerance: 0.1        # Distance to consider goal reached (meters)
